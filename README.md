# Authentication-system-secret-file
An authentication which uses hashing and salting for encryption and flask to let only registered and logged users (authenticated users) to download the secret file.

Create a folder 'instances' to store all the databases for you application. For eg. 'Users' in this case.
